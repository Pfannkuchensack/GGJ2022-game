export default class Game {
    constructor({
        canvas,
        socket
    }) {
        this._canvas = canvas;
        this._ctx = canvas.getContext('2d');
        this._socket = socket;
    }
    start() {
        // render tiles

        // render chars

    }
}
