export default class Render {
    constructor(canvas) {
        this._canvas = canvas;
        this._ctx = canvas.getContext('2d');
    }
    render() {
        // render tiles

        // render chars

    }
}
